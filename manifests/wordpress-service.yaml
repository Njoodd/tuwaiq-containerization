apiVersion: v1
kind: Service
metadata:
  name: wordpress-svc
  namespace: njood-ns
  labels:
    app: wordpress
spec:
  ports:
    - port: 8888
  selector:
    app: wordpress
    tier: frontend
  type: LoadBalancer